<%# <h2>THis is index page OR default page,where display all tweets!!</h2> %>

<section class="section">
	<div class="container">
		<div class="columns">
			<%= render 'feed' %>
			<%= render 'who-to-follow' %>
	    </div>
	</div>
</section>
		
